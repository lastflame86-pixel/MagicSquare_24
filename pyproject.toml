[project]
name = "magicsquare"
version = "0.1.0"
description = "4x4 Magic Square validation — QA TDD project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"

[tool.coverage.run]
source = ["magicsquare"]
